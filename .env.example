NODE_ENV=production
PORT=8787

# PostgreSQL（Railway 自动提供变量，也可手动填）
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require

# CORS：你的前端源（多个用逗号，无空格）
CORS_ORIGIN=https://andyming13.github.io,https://www.inscopei1.com.au,http://localhost:8000

# JWT
JWT_SECRET=please_replace_with_a_long_random_secret

# 邮件（后续如需）
EMAIL_MODE=resend
RESEND_API_KEY= InScope <noreply@inscopei1.com.au>
EMAIL_FROM="InScope <noreply@inscopei1.com.au>"

# DB 证书校验（如云平台缺根证书可设为 false）
DB_SSL_STRICT=true